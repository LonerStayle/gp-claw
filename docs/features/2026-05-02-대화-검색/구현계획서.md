# 대화 검색 구현계획서

> **For agentic workers:** REQUIRED SUB-SKILL: Use `subagent-driven-development` (recommended) or `executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** GP Claw 대화방의 메시지를 키워드 + 필터(날짜·방·role)로 검색하고, 결과 클릭 시 해당 방으로 점프해 메시지를 하이라이트한다.

**Architecture:** LangGraph 체크포인트 BLOB은 그대로 두고 신규 `messages` 테이블에 mirror write. WS `user_message` 핸들러에서 dual-write, REST `/search/messages`·`/search/rooms`로 조회. 프론트엔드는 상단 `SearchBar` + 결과 패널 + 사이드바 방 이름 필터 + `ChatContainer` 점프/하이라이트.

**Tech Stack:** Python 3.x · FastAPI · aiosqlite (async) / sqlite3 (sync) · pytest · React + TypeScript + Vite · Tailwind

**Spec inputs:**
- 요구사항.md — FR-1..9, NFR-1..3, AC-1..7
- 개발방향.md — Dual-store mirror 아키텍처, LIKE + 인덱스, WS 핸들러 dual-write, 상단 SearchBar UX, 위험 R-1..R-7

---

## 1. 단계별 작업

### Task 1: MessageStore — 신규 SQLite 메시지 미러 클래스

**Files:**
- Create: `src/gp_claw/messages.py`
- Test: `tests/test_messages.py`

**FR/Decision 매핑:** FR-1, FR-2, FR-3, FR-5, FR-6, FR-7 / 개발방향 §3, §5 결정 1·3
**위험 매핑:** R-1 (side-effect), R-2 (race), R-5 (side-effect)

- [ ] **Step 1: 테스트 — 빈 store에서 search 시 0건 반환**

```python
# tests/test_messages.py
import sqlite3
import pytest
from gp_claw.messages import MessageStore


@pytest.fixture
def store(tmp_path):
    db = tmp_path / "test.db"
    s = MessageStore(str(db))
    yield s
    s.close()


def test_empty_search_returns_zero(store):
    result = store.search(q="hello")
    assert result["total"] == 0
    assert result["items"] == []
```

- [ ] **Step 2: Run test — fail**

```bash
pytest tests/test_messages.py::test_empty_search_returns_zero -v
```

Expected: `ImportError: No module named 'gp_claw.messages'`

- [ ] **Step 3: 최소 구현 — MessageStore 골격 + CREATE TABLE**

```python
# src/gp_claw/messages.py
"""Mirror store for searchable messages — SQLite 기반."""

import re
import sqlite3
from datetime import datetime, timezone
from typing import Iterable


_TOOL_TAG_RE = re.compile(r"</?tool_call>.*", re.DOTALL)


def _now_iso() -> str:
    return datetime.now(timezone.utc).isoformat()


def _clean(text: str) -> str:
    return _TOOL_TAG_RE.sub("", text or "").strip()


class MessageStore:
    """Mirror table for full-text-ish keyword search across all rooms."""

    def __init__(self, db_path: str = ":memory:") -> None:
        self._conn = sqlite3.connect(db_path, check_same_thread=False)
        self._conn.row_factory = sqlite3.Row
        self._conn.execute("PRAGMA journal_mode=WAL")
        self._create_schema()

    def _create_schema(self) -> None:
        self._conn.executescript("""
            CREATE TABLE IF NOT EXISTS messages (
                id          INTEGER PRIMARY KEY AUTOINCREMENT,
                room_id     TEXT    NOT NULL,
                role        TEXT    NOT NULL,
                content     TEXT    NOT NULL,
                created_at  TEXT    NOT NULL,
                seq         INTEGER NOT NULL
            );
            CREATE INDEX IF NOT EXISTS idx_messages_created_at ON messages(created_at DESC);
            CREATE INDEX IF NOT EXISTS idx_messages_room_id    ON messages(room_id);
            CREATE INDEX IF NOT EXISTS idx_messages_role       ON messages(role);
            CREATE UNIQUE INDEX IF NOT EXISTS idx_messages_room_seq ON messages(room_id, seq);
        """)
        self._conn.commit()

    def search(
        self,
        q: str,
        room_ids: list[str] | None = None,
        roles: list[str] | None = None,
        date_from: str | None = None,
        date_to: str | None = None,
        limit: int = 50,
        offset: int = 0,
    ) -> dict:
        return {"total": 0, "items": []}

    def close(self) -> None:
        self._conn.close()
```

- [ ] **Step 4: Run test — pass**

```bash
pytest tests/test_messages.py::test_empty_search_returns_zero -v
```

Expected: PASS

- [ ] **Step 5: 테스트 — append 후 search 결과 1건**

```python
# tests/test_messages.py
def test_append_then_search_finds_match(store):
    store.append(room_id="r1", role="user", content="hello world")
    result = store.search(q="hello")
    assert result["total"] == 1
    assert len(result["items"]) == 1
    item = result["items"][0]
    assert item["room_id"] == "r1"
    assert item["role"] == "user"
    assert item["content"] == "hello world"
    assert "hello" in item["snippet"].lower()
    assert isinstance(item["match_offsets"], list)
    assert item["match_offsets"][0] == [0, 5]
```

- [ ] **Step 6: Run — fail (`AttributeError: append`)**

```bash
pytest tests/test_messages.py::test_append_then_search_finds_match -v
```

- [ ] **Step 7: append + search 본구현**

```python
# src/gp_claw/messages.py — replace search/add append
    def append(
        self,
        room_id: str,
        role: str,
        content: str,
        created_at: str | None = None,
    ) -> int | None:
        # ⚠️ RISK(side-effect): <tool_call> 정제 — by R-5
        cleaned = _clean(content)
        if not cleaned:
            return None
        ts = created_at or _now_iso()
        # ⚠️ RISK(race): seq를 트랜잭션 내에서 MAX+1 — by R-2
        try:
            with self._conn:
                row = self._conn.execute(
                    "SELECT COALESCE(MAX(seq), 0) AS m FROM messages WHERE room_id = ?",
                    (room_id,),
                ).fetchone()
                next_seq = (row["m"] or 0) + 1
                cur = self._conn.execute(
                    """INSERT INTO messages (room_id, role, content, created_at, seq)
                       VALUES (?, ?, ?, ?, ?)""",
                    (room_id, role, cleaned, ts, next_seq),
                )
                return cur.lastrowid
        except sqlite3.IntegrityError:
            # seq 충돌 시 1회 재시도 (단일 사용자 가정 — 거의 발생 안함)
            return self.append(room_id, role, content, created_at)

    def search(
        self,
        q: str,
        room_ids: list[str] | None = None,
        roles: list[str] | None = None,
        date_from: str | None = None,
        date_to: str | None = None,
        limit: int = 50,
        offset: int = 0,
    ) -> dict:
        clauses = ["LOWER(content) LIKE ?"]
        params: list = [f"%{q.lower()}%"]
        if room_ids:
            placeholders = ",".join(["?"] * len(room_ids))
            clauses.append(f"room_id IN ({placeholders})")
            params.extend(room_ids)
        if roles:
            placeholders = ",".join(["?"] * len(roles))
            clauses.append(f"role IN ({placeholders})")
            params.extend(roles)
        if date_from:
            clauses.append("created_at >= ?")
            params.append(date_from)
        if date_to:
            clauses.append("created_at <= ?")
            params.append(date_to)
        where = " AND ".join(clauses)
        total = self._conn.execute(
            f"SELECT COUNT(*) AS c FROM messages WHERE {where}", params
        ).fetchone()["c"]
        rows = self._conn.execute(
            f"""SELECT id, room_id, role, content, created_at
                FROM messages WHERE {where}
                ORDER BY created_at DESC, id DESC
                LIMIT ? OFFSET ?""",
            params + [limit, offset],
        ).fetchall()
        items = [self._row_to_item(r, q) for r in rows]
        return {"total": total, "items": items}

    @staticmethod
    def _row_to_item(row: sqlite3.Row, q: str) -> dict:
        content: str = row["content"]
        lower = content.lower()
        ql = q.lower()
        idx = lower.find(ql)
        if idx >= 0:
            start = max(0, idx - 50)
            end = min(len(content), idx + len(q) + 50)
            snippet = ("…" if start > 0 else "") + content[start:end] + ("…" if end < len(content) else "")
            offsets = [[idx, idx + len(q)]]
        else:
            snippet = content[:120]
            offsets = []
        return {
            "id": row["id"],
            "room_id": row["room_id"],
            "role": row["role"],
            "content": content,
            "snippet": snippet,
            "match_offsets": offsets,
            "created_at": row["created_at"],
        }
```

- [ ] **Step 8: Run — pass**

```bash
pytest tests/test_messages.py -v
```

- [ ] **Step 9: 추가 테스트 — 필터 조합 + tool_call 정제 + 페이지네이션**

```python
# tests/test_messages.py
def test_role_filter(store):
    store.append("r1", "user", "alpha")
    store.append("r1", "assistant", "alpha")
    res = store.search(q="alpha", roles=["user"])
    assert res["total"] == 1
    assert res["items"][0]["role"] == "user"


def test_room_filter(store):
    store.append("r1", "user", "x")
    store.append("r2", "user", "x")
    res = store.search(q="x", room_ids=["r2"])
    assert res["total"] == 1
    assert res["items"][0]["room_id"] == "r2"


def test_date_range(store):
    store.append("r1", "user", "old", created_at="2026-01-01T00:00:00+00:00")
    store.append("r1", "user", "new", created_at="2026-05-01T00:00:00+00:00")
    res = store.search(q="", date_from="2026-04-01T00:00:00+00:00")
    # q="" matches everything via LIKE '%%'
    assert res["total"] == 1
    assert res["items"][0]["content"] == "new"


def test_tool_call_stripped(store):
    rid = store.append("r1", "assistant", "answer\n<tool_call>{...}</tool_call>")
    rows = store._conn.execute("SELECT content FROM messages WHERE id=?", (rid,)).fetchall()
    assert rows[0]["content"] == "answer"


def test_empty_assistant_skipped(store):
    rid = store.append("r1", "assistant", "<tool_call>x</tool_call>")
    assert rid is None


def test_case_insensitive_partial(store):
    store.append("r1", "user", "Reactive Programming")
    res = store.search(q="REACT")
    assert res["total"] == 1


def test_pagination(store):
    for i in range(5):
        store.append("r1", "user", f"item {i}", created_at=f"2026-05-0{i+1}T00:00:00+00:00")
    page1 = store.search(q="item", limit=2, offset=0)
    page2 = store.search(q="item", limit=2, offset=2)
    assert page1["total"] == 5
    assert page2["total"] == 5
    assert len(page1["items"]) == 2 and len(page2["items"]) == 2
    assert page1["items"][0]["content"] == "item 4"  # latest first
    assert page2["items"][0]["content"] == "item 2"
```

- [ ] **Step 10: Run all messages tests — pass**

```bash
pytest tests/test_messages.py -v
```

Expected: 8 passed

- [ ] **Step 11: NFR-1 sanity 측정 — 1만 mock 메시지로 LIKE 1초 미만 확인 (선택, 1회성)**

```python
# tests/test_messages.py
import time

def test_search_under_1s_with_10k_rows(store):
    # 1만 row inject
    for i in range(10_000):
        store.append("r1", "user", f"line {i} keyword {i % 100}")
    t0 = time.perf_counter()
    res = store.search(q="keyword", limit=50)
    dt = time.perf_counter() - t0
    assert res["total"] > 0
    assert dt < 1.0, f"LIKE took {dt:.2f}s — exceeds 1s sanity bound"
```

```bash
pytest tests/test_messages.py::test_search_under_1s_with_10k_rows -v
```

> 개발방향 §7 NFR 검증의 sanity 측정 — 1회 실행하고 향후 정기 실행은 안 함. 실패 시 NFR-1 재논의 트리거.

- [ ] **Step 12: Commit**

```bash
git add src/gp_claw/messages.py tests/test_messages.py
git commit -m "feat: MessageStore mirror table with LIKE search + filters"
```

---

### Task 2: Server lifespan + dual-write + get_room_messages id 필드

**Files:**
- Modify: `src/gp_claw/server.py` (lifespan, `user_message` 핸들러, `get_room_messages`)
- Test: `tests/test_server.py`, `tests/test_ws_agent.py` (회귀)

**FR/Decision 매핑:** FR-1, FR-2, FR-4 / 개발방향 §5 결정 1·3
**위험 매핑:** R-1 (side-effect dual-write), R-7 (breaking response schema)

> ℹ️ Fixture 의존성: `client_with_room_and_messages` fixture는 Task 3 Step 6에서 노출되는 `app.state.message_store`를 통해 직접 `append`한다. 따라서 Task 2의 회귀 테스트 fixture 구현은 Task 3 Step 6의 변경(`_app.state.message_store = _msg_store[0]`)을 함께 둔 상태에서만 동작 — Task 2와 Task 3은 사실상 같은 PR에서 진행하며, 임시로 fixture 안에서 `MessageStore(db_path)`를 직접 인스턴스화해 inject해도 무방.

- [ ] **Step 1: 회귀 테스트 — `get_room_messages` 응답에 `id` 필드 + 단조 증가 (R-7)**

```python
# tests/test_server.py — 신규 테스트 추가
def test_get_room_messages_returns_id_field(client_with_room_and_messages):
    """R-7 회귀: id 필드 존재 + 메시지 순서대로 단조 증가."""
    client, room_id = client_with_room_and_messages
    r = client.get(f"/rooms/{room_id}/messages")
    assert r.status_code == 200
    msgs = r.json()
    assert len(msgs) >= 2
    assert all("id" in m for m in msgs)
    ids = [m["id"] for m in msgs]
    assert ids == sorted(ids), f"id가 단조 증가하지 않음: {ids}"
```

(`client_with_room_and_messages` fixture는 기존 `tests/conftest.py` 패턴을 참고해 추가. 이 fixture가 없으면 함께 신설.)

- [ ] **Step 2: Run — fail (id 필드 없음)**

```bash
pytest tests/test_server.py::test_get_room_messages_returns_id_field -v
```

- [ ] **Step 3: lifespan에 MessageStore 인스턴스 추가 + `get_room_messages` 응답 확장**

`src/gp_claw/server.py`:

```python
# 상단 import 추가
from gp_claw.messages import MessageStore

# create_app() 안 _checkpointer/_agent 옆에 추가
_msg_store = [None]  # type: ignore[var-annotated]
```

```python
# lifespan 함수 본문 보강
@asynccontextmanager
async def lifespan(_app: FastAPI):
    conn = await aiosqlite.connect(db_path)
    await conn.execute("PRAGMA journal_mode=WAL")
    _checkpointer[0] = AsyncSqliteSaver(conn)
    await _checkpointer[0].setup()
    _agent[0] = create_agent(llm, registry=registry, checkpointer=_checkpointer[0]) if llm else None
    _msg_store[0] = MessageStore(db_path)  # 같은 DB 파일 사용 — 별도 테이블
    yield
    _msg_store[0].close()
    await conn.close()
```

```python
# get_room_messages — id/role/created_at을 함께 반환 (FR-4 점프 + R-7)
@app.get("/rooms/{room_id}/messages")
async def get_room_messages(room_id: str):
    room = room_manager.get(room_id)
    if not room:
        return JSONResponse(status_code=404, content={"detail": "Room not found"})
    if not _msg_store[0]:
        return []
    # MessageStore mirror에서 직접 읽음 — 체크포인트 BLOB 디시리얼라이즈 회피
    rows = _msg_store[0]._conn.execute(
        """SELECT id, role, content, created_at FROM messages
           WHERE room_id = ? ORDER BY seq ASC""",
        (room_id,),
    ).fetchall()
    return [
        {
            "id": r["id"],
            "type": r["role"],          # 기존 응답과 호환: 'user'/'assistant'
            "content": r["content"],
            "created_at": r["created_at"],
        }
        for r in rows
    ]
```

> ⚠️ RISK(breaking): `_msg_store[0]._conn` 직접 접근은 일시적 — 다음 step에서 `MessageStore.list_by_room()` 메서드로 추출. 그 전까지는 테스트 통과를 우선.

- [ ] **Step 4: Run — pass (id 필드 포함)**

```bash
pytest tests/test_server.py::test_get_room_messages_returns_id_field -v
```

- [ ] **Step 5: `MessageStore.list_by_room()` 추출**

```python
# src/gp_claw/messages.py
def list_by_room(self, room_id: str) -> list[dict]:
    rows = self._conn.execute(
        """SELECT id, role, content, created_at FROM messages
           WHERE room_id = ? ORDER BY seq ASC""",
        (room_id,),
    ).fetchall()
    return [
        {"id": r["id"], "type": r["role"], "content": r["content"],
         "created_at": r["created_at"]}
        for r in rows
    ]
```

`server.py`의 `get_room_messages` 본문을:

```python
return _msg_store[0].list_by_room(room_id)
```

로 교체.

- [ ] **Step 6: 테스트 — `MessageStore.list_by_room`**

```python
# tests/test_messages.py
def test_list_by_room_orders_by_seq(store):
    store.append("r1", "user", "first")
    store.append("r1", "assistant", "second")
    store.append("r2", "user", "other")
    msgs = store.list_by_room("r1")
    assert [m["content"] for m in msgs] == ["first", "second"]
    assert all("id" in m and "type" in m and "created_at" in m for m in msgs)
```

- [ ] **Step 7: WS `user_message` 핸들러에 dual-write 추가**

`src/gp_claw/server.py` `user_message` 처리 분기 끝(또는 `room_manager.touch` 직후)에:

```python
# Mirror write — user 메시지 (R-1: try/except로 본 흐름 보호)
try:
    if _msg_store[0]:
        _msg_store[0].append(room_id=session_id, role="user", content=content)
except Exception as e:
    logger.warning(f"MessageStore.append(user) failed: {e}")
```

그리고 응답 완료 후(스트리밍 끝, file_card 처리 직후) AI 응답을 final_state에서 추출하여 mirror:

```python
# Mirror write — assistant 메시지들 (마지막 user 이후의 모든 새 메시지)
try:
    if _msg_store[0]:
        for m in recent_msgs:
            role = None
            if isinstance(m, AIMessage):
                role = "assistant"
            elif hasattr(m, "type") and m.type == "tool":
                role = "tool"
            if role and getattr(m, "content", None):
                _msg_store[0].append(
                    room_id=session_id,
                    role=role,
                    content=str(m.content),
                )
except Exception as e:
    logger.warning(f"MessageStore.append(assistant/tool) failed: {e}")
```

- [ ] **Step 8: 통합 테스트 — WS 메시지 후 search/list_by_room으로 mirror 확인**

```python
# tests/test_ws_agent.py — 기존 mock LLM 시나리오 끝에 추가 검증
def test_ws_dual_write_to_message_store(test_client_with_mock_llm):
    client = test_client_with_mock_llm
    with client.websocket_connect("/ws/room-x") as ws:
        ws.send_json({"type": "user_message", "content": "hello mirror"})
        # 응답 수신 …(생략)
    # REST로 검증
    msgs = client.get("/rooms/room-x/messages").json()
    assert any(m["type"] == "user" and m["content"] == "hello mirror" for m in msgs)
```

- [ ] **Step 9: Run all server tests**

```bash
pytest tests/test_server.py tests/test_ws_agent.py tests/test_messages.py -v
```

- [ ] **Step 10: Commit**

```bash
git add src/gp_claw/server.py src/gp_claw/messages.py tests/test_server.py tests/test_ws_agent.py tests/test_messages.py
git commit -m "feat: WS dual-write to MessageStore + id field on /rooms/{id}/messages"
```

---

### Task 3: REST 검색 엔드포인트 — `/search/messages` + `/search/rooms`

**Files:**
- Modify: `src/gp_claw/server.py` (엔드포인트 2개 추가)
- Test: `tests/test_search_api.py` (신규)

**FR/Decision 매핑:** FR-1, FR-3, FR-5, FR-6, FR-7, FR-8, FR-9 / 개발방향 §4
**위험 매핑:** R-6 (perf — accept), R-4 (perf — accept)

- [ ] **Step 1: 테스트 — 빈 store에 GET `/search/messages?q=x` → total=0**

```python
# tests/test_search_api.py
import pytest
from fastapi.testclient import TestClient
from gp_claw.server import create_app


@pytest.fixture
def client(tmp_path):
    db = tmp_path / "search.db"
    app = create_app(llm=None, db_path=str(db))
    with TestClient(app) as c:
        yield c


def test_search_messages_empty(client):
    r = client.get("/search/messages", params={"q": "anything"})
    assert r.status_code == 200
    body = r.json()
    assert body["total"] == 0
    assert body["items"] == []
```

- [ ] **Step 2: Run — fail (404)**

```bash
pytest tests/test_search_api.py::test_search_messages_empty -v
```

- [ ] **Step 3: `/search/messages` 엔드포인트 구현**

```python
# src/gp_claw/server.py
from fastapi import Query

@app.get("/search/messages")
async def search_messages(
    q: str = Query(..., min_length=1),
    room_id: list[str] | None = Query(default=None),
    role: list[str] | None = Query(default=None),
    date_from: str | None = Query(default=None, alias="from"),
    date_to: str | None = Query(default=None, alias="to"),
    limit: int = Query(default=50, ge=1, le=200),
    offset: int = Query(default=0, ge=0),
):
    if not _msg_store[0]:
        return {"total": 0, "items": []}
    result = _msg_store[0].search(
        q=q, room_ids=room_id, roles=role,
        date_from=date_from, date_to=date_to,
        limit=limit, offset=offset,
    )
    # 결과 항목에 room_title 채워넣기 (FR-3)
    titles = {r.id: r.title for r in room_manager.list_all()}
    for it in result["items"]:
        it["room_title"] = titles.get(it["room_id"], "")
    return result
```

- [ ] **Step 4: Run — pass**

```bash
pytest tests/test_search_api.py::test_search_messages_empty -v
```

- [ ] **Step 5: 테스트 — 필터 조합 + room_title 포함**

```python
# tests/test_search_api.py
def test_search_messages_with_data_and_filters(client):
    # 방 생성
    r1 = client.post("/rooms", json={"title": "회의"}).json()
    rid1 = r1["id"]
    # 직접 store에 inject (mock LLM 없이)
    from gp_claw.server import create_app  # noqa: F401
    # WS 통하지 않고 messages 테이블에 직접 append하기 위해 별도 fixture 사용
    # — 구현시 client fixture가 app.state로 store 노출하도록 보강
    ...

def test_search_messages_zero_match(client):
    r = client.get("/search/messages", params={"q": "nonexistent_token"})
    assert r.status_code == 200
    assert r.json()["total"] == 0


def test_search_messages_invalid_q_empty(client):
    r = client.get("/search/messages", params={"q": ""})
    assert r.status_code == 422
```

> 주의: 두 번째 테스트(`test_search_messages_with_data_and_filters`)를 위해 `client` fixture에 `app.state.message_store = _msg_store[0]` (lifespan 내부)을 노출하거나, fixture에서 직접 `MessageStore(db_path)`를 생성해 inject. 구현 시 일관된 방법 선택.

- [ ] **Step 6: `app.state.message_store` 노출 + 데이터 inject 테스트 추가**

```python
# server.py lifespan 내부 yield 직전에:
_app.state.message_store = _msg_store[0]
_app.state.room_manager = room_manager
```

```python
# tests/test_search_api.py
def test_search_messages_role_and_room_filter(client):
    rm = client.app.state.room_manager
    store = client.app.state.message_store
    rA = rm.create(title="방A").id
    rB = rm.create(title="방B").id
    store.append(rA, "user", "alpha keyword")
    store.append(rA, "assistant", "alpha keyword")
    store.append(rB, "user", "alpha keyword")

    r = client.get("/search/messages", params={
        "q": "keyword", "room_id": [rA], "role": ["user"]
    })
    body = r.json()
    assert body["total"] == 1
    assert body["items"][0]["room_id"] == rA
    assert body["items"][0]["role"] == "user"
    assert body["items"][0]["room_title"] == "방A"
```

- [ ] **Step 7: `/search/rooms` 엔드포인트 + 테스트**

```python
# src/gp_claw/server.py
@app.get("/search/rooms")
async def search_rooms(q: str = Query(default="")):
    rooms = room_manager.list_all()
    if not q:
        return [asdict(r) for r in rooms]
    ql = q.lower()
    return [asdict(r) for r in rooms if ql in r.title.lower()]
```

```python
# tests/test_search_api.py
def test_search_rooms_partial_case_insensitive(client):
    client.post("/rooms", json={"title": "리팩토링 회의"})
    client.post("/rooms", json={"title": "DESIGN 검토"})
    r = client.get("/search/rooms", params={"q": "design"})
    titles = [x["title"] for x in r.json()]
    assert "DESIGN 검토" in titles
    assert "리팩토링 회의" not in titles
```

- [ ] **Step 8: Run all search tests**

```bash
pytest tests/test_search_api.py -v
```

- [ ] **Step 9: Commit**

```bash
git add src/gp_claw/server.py tests/test_search_api.py
git commit -m "feat: /search/messages and /search/rooms REST endpoints"
```

---

### Task 4: Backfill 스크립트

**Files:**
- Create: `scripts/__init__.py` (빈 파일, 없으면)
- Create: `scripts/backfill_messages.py`
- Test: `tests/test_backfill.py`

**FR/Decision 매핑:** 마이그레이션 (개발방향 §3)
**위험 매핑:** R-3 (breaking — 기존 사용자 빈 검색결과)

- [ ] **Step 1: 테스트 — 빈 DB 백필 후 0 rows**

```python
# tests/test_backfill.py
import asyncio
import pytest
from gp_claw.messages import MessageStore
from scripts.backfill_messages import backfill


@pytest.fixture
def db(tmp_path):
    return str(tmp_path / "bf.db")


def test_backfill_empty_db_is_zero(db):
    # rooms 테이블 미존재 → 안전 처리
    count = asyncio.run(backfill(db))
    assert count == 0
```

- [ ] **Step 2: Run — fail (script 없음)**

```bash
pytest tests/test_backfill.py::test_backfill_empty_db_is_zero -v
```

- [ ] **Step 3: 백필 스크립트 작성**

```python
# scripts/__init__.py — 빈 파일

# scripts/backfill_messages.py
"""기존 LangGraph 체크포인트에서 messages 테이블로 1회 마이그레이션."""

import asyncio
import sys
from datetime import datetime, timezone

import aiosqlite
from langchain_core.messages import AIMessage, HumanMessage, SystemMessage, ToolMessage
from langgraph.checkpoint.sqlite.aio import AsyncSqliteSaver

from gp_claw.messages import MessageStore
from gp_claw.rooms import RoomManager


def _role_of(msg) -> str | None:
    if isinstance(msg, HumanMessage):
        return "user"
    if isinstance(msg, AIMessage):
        return "assistant"
    if isinstance(msg, ToolMessage):
        return "tool"
    if isinstance(msg, SystemMessage):
        return "system"
    return None


async def backfill(db_path: str) -> int:
    """모든 방을 순회하며 메시지를 messages 테이블에 적재. 멱등.

    Returns: appended row count.
    """
    rm = RoomManager(db_path)
    rooms = rm.list_all()
    rm.close()
    if not rooms:
        return 0

    store = MessageStore(db_path)
    # 기존 row 정리(멱등 보장: 부분 적재 후 재실행 케이스 대비)
    store._conn.execute("DELETE FROM messages")
    store._conn.commit()

    appended = 0
    conn = await aiosqlite.connect(db_path)
    try:
        saver = AsyncSqliteSaver(conn)
        await saver.setup()
        for room in rooms:
            try:
                cps = saver.alist({"configurable": {"thread_id": room.id}})
                latest = None
                async for cp in cps:
                    latest = cp
                    break
                if latest is None:
                    continue
                state_msgs = latest.checkpoint.get("channel_values", {}).get("messages", [])
                for m in state_msgs:
                    role = _role_of(m)
                    if not role:
                        continue
                    rid = store.append(
                        room_id=room.id,
                        role=role,
                        content=getattr(m, "content", "") or "",
                        created_at=datetime.now(timezone.utc).isoformat(),
                    )
                    if rid is not None:
                        appended += 1
            except Exception as e:
                print(f"[backfill] room={room.id} failed: {e}", file=sys.stderr)
    finally:
        await conn.close()
        store.close()
    return appended


def _main() -> None:
    if len(sys.argv) < 2:
        print("usage: python -m scripts.backfill_messages <db_path>")
        sys.exit(1)
    n = asyncio.run(backfill(sys.argv[1]))
    print(f"backfill complete: {n} messages appended")


if __name__ == "__main__":
    _main()
```

- [ ] **Step 4: Run — pass (빈 DB)**

```bash
pytest tests/test_backfill.py::test_backfill_empty_db_is_zero -v
```

- [ ] **Step 5: 멱등성 테스트 — 두 번 실행 결과 동일**

```python
# tests/test_backfill.py
def test_backfill_is_idempotent(db):
    rm = RoomManager(db); rm.create(room_id="r1", title="t"); rm.close()
    store = MessageStore(db)
    store.append("r1", "user", "manual1")
    store.append("r1", "assistant", "manual2")
    store.close()
    asyncio.run(backfill(db))
    asyncio.run(backfill(db))
    s2 = MessageStore(db)
    msgs = s2.list_by_room("r1")
    s2.close()
    # 백필은 체크포인트 소스만 사용하므로 manually inject한 row는 사라지고
    # 빈 체크포인트일 경우 0개. 같은 결과를 두 번 보장.
    assert len(msgs) == 0
```

> 메모: 진짜 체크포인트가 있는 시나리오 테스트는 통합 테스트(WS로 메시지 보낸 뒤 백필) — 후속 보강 가능. 본 단계는 멱등성과 안전성만.

- [ ] **Step 6: Run — pass**

```bash
pytest tests/test_backfill.py -v
```

- [ ] **Step 7: Commit**

```bash
git add scripts/__init__.py scripts/backfill_messages.py tests/test_backfill.py
git commit -m "feat: backfill_messages script (idempotent)"
```

---

### Task 5: Frontend 타입 + 검색 hook

**Files:**
- Modify: `frontend/src/types.ts` (SearchResult, SearchFilter, Message에 id를 number-friendly로 통일)
- Create: `frontend/src/hooks/useSearch.ts`

**FR/Decision 매핑:** FR-1, FR-3, FR-6, FR-7
**위험 매핑:** R-7 (Message.id 타입 호환)

- [ ] **Step 1: types.ts — `SearchResult`/`SearchResponse`/`SearchFilter` 추가**

```typescript
// frontend/src/types.ts — 끝에 추가
export type MessageRole = "user" | "assistant" | "tool" | "system"

export interface SearchResultItem {
  id: number
  room_id: string
  room_title: string
  role: MessageRole
  content: string
  snippet: string
  match_offsets: [number, number][]
  created_at: string
}

export interface SearchResponse {
  total: number
  items: SearchResultItem[]
}

export interface SearchFilter {
  q: string
  roomIds: string[]
  roles: MessageRole[]
  dateFrom?: string  // ISO
  dateTo?: string    // ISO
}
```

(기존 `Message` 타입의 `id: string`은 그대로 유지 — 프론트엔드 내부 ID와 백엔드 mirror id를 분리 운영. 점프 시 별도 `serverMessageId?: number` 필드 추가:)

```typescript
// UserMessage / AssistantMessage 인터페이스에 동일하게 추가
  serverMessageId?: number  // /rooms/{id}/messages 응답의 id (R-7 / FR-4 점프용)
```

- [ ] **Step 2: useSearch hook — 디바운스 + fetch**

```typescript
// frontend/src/hooks/useSearch.ts
import { useEffect, useState } from "react"
import type { SearchFilter, SearchResponse } from "@/types"

const PAGE_SIZE = 50

export function useSearch(filter: SearchFilter) {
  const [data, setData] = useState<SearchResponse | null>(null)
  const [loading, setLoading] = useState(false)
  const [error, setError] = useState<string | null>(null)
  const [offset, setOffset] = useState(0)

  useEffect(() => { setOffset(0) }, [filter.q, filter.roomIds, filter.roles, filter.dateFrom, filter.dateTo])

  useEffect(() => {
    if (!filter.q || filter.q.length === 0) {
      setData(null)
      return
    }
    const handle = setTimeout(async () => {
      setLoading(true); setError(null)
      try {
        const params = new URLSearchParams()
        params.set("q", filter.q)
        filter.roomIds.forEach(r => params.append("room_id", r))
        filter.roles.forEach(r => params.append("role", r))
        if (filter.dateFrom) params.set("from", filter.dateFrom)
        if (filter.dateTo) params.set("to", filter.dateTo)
        params.set("limit", String(PAGE_SIZE))
        params.set("offset", String(offset))
        const res = await fetch(`/search/messages?${params}`)
        if (!res.ok) throw new Error(`${res.status}`)
        const body: SearchResponse = await res.json()
        // append-mode for offset>0, replace for offset=0
        setData(prev => offset === 0 || !prev
          ? body
          : { total: body.total, items: [...prev.items, ...body.items] })
      } catch (e) {
        setError(e instanceof Error ? e.message : String(e))
      } finally {
        setLoading(false)
      }
    }, 250)
    return () => clearTimeout(handle)
  }, [filter.q, filter.roomIds, filter.roles, filter.dateFrom, filter.dateTo, offset])

  return {
    data,
    loading,
    error,
    canLoadMore: data ? data.items.length < data.total : false,
    loadMore: () => setOffset(o => o + PAGE_SIZE),
  }
}
```

- [ ] **Step 3: TS 빌드 검증**

```bash
cd frontend && npm run build
```

Expected: 0 errors

- [ ] **Step 4: Commit**

```bash
git add frontend/src/types.ts frontend/src/hooks/useSearch.ts
git commit -m "feat: useSearch hook + SearchResult/SearchFilter types"
```

---

### Task 6: SearchBar 컴포넌트

**Files:**
- Create: `frontend/src/components/SearchBar.tsx`

**FR/Decision 매핑:** FR-1, FR-7

- [ ] **Step 1: SearchBar — controlled input + 필터 토글 UI**

```typescript
// frontend/src/components/SearchBar.tsx
import { Search } from "lucide-react"
import type { MessageRole, SearchFilter } from "@/types"
import type { Room } from "@/types"

interface SearchBarProps {
  rooms: Room[]
  filter: SearchFilter
  onChange: (next: SearchFilter) => void
  onClear: () => void
}

const ROLES: MessageRole[] = ["user", "assistant", "tool", "system"]
const ROLE_LABELS: Record<MessageRole, string> = {
  user: "내가 보낸",
  assistant: "AI 응답",
  tool: "도구",
  system: "시스템",
}

const QUICK_RANGES: { label: string; days: number | null }[] = [
  { label: "전체", days: null },
  { label: "오늘", days: 0 },
  { label: "최근 7일", days: 7 },
  { label: "최근 30일", days: 30 },
]

function isoDaysAgo(days: number): string {
  const d = new Date()
  d.setHours(0, 0, 0, 0)
  d.setDate(d.getDate() - days)
  return d.toISOString()
}

export function SearchBar({ rooms, filter, onChange, onClear }: SearchBarProps) {
  const toggleRole = (role: MessageRole) => {
    const next = filter.roles.includes(role)
      ? filter.roles.filter(r => r !== role)
      : [...filter.roles, role]
    onChange({ ...filter, roles: next })
  }
  const toggleRoom = (id: string) => {
    const next = filter.roomIds.includes(id)
      ? filter.roomIds.filter(r => r !== id)
      : [...filter.roomIds, id]
    onChange({ ...filter, roomIds: next })
  }
  const setRange = (days: number | null) => {
    if (days === null) {
      onChange({ ...filter, dateFrom: undefined, dateTo: undefined })
    } else {
      onChange({ ...filter, dateFrom: isoDaysAgo(days), dateTo: undefined })
    }
  }

  return (
    <div className="border-b bg-background p-3 space-y-2">
      <div className="flex items-center gap-2">
        <Search className="h-4 w-4 text-muted-foreground" />
        <input
          type="text"
          value={filter.q}
          onChange={e => onChange({ ...filter, q: e.target.value })}
          placeholder="대화 전체에서 검색…"
          className="flex-1 rounded border px-2 py-1 text-sm outline-none focus:ring"
          aria-label="검색어"
        />
        {filter.q && (
          <button onClick={onClear} className="text-xs text-muted-foreground underline">
            지우기
          </button>
        )}
      </div>

      {filter.q && (
        <div className="flex flex-wrap gap-2 text-xs">
          {ROLES.map(r => (
            <label key={r} className="flex items-center gap-1">
              <input
                type="checkbox"
                checked={filter.roles.includes(r)}
                onChange={() => toggleRole(r)}
              />
              {ROLE_LABELS[r]}
            </label>
          ))}
          <span className="mx-2 text-muted-foreground">|</span>
          {QUICK_RANGES.map(qr => (
            <button
              key={qr.label}
              onClick={() => setRange(qr.days)}
              className="rounded border px-2 py-0.5 hover:bg-accent"
            >
              {qr.label}
            </button>
          ))}
          <span className="mx-2 text-muted-foreground">|</span>
          <details className="relative">
            <summary className="cursor-pointer">방 필터({filter.roomIds.length || "전체"})</summary>
            <div className="absolute z-10 mt-1 max-h-48 w-56 overflow-y-auto rounded border bg-background p-2 shadow">
              {rooms.map(r => (
                <label key={r.id} className="flex items-center gap-1 py-0.5">
                  <input
                    type="checkbox"
                    checked={filter.roomIds.includes(r.id)}
                    onChange={() => toggleRoom(r.id)}
                  />
                  <span className="truncate">{r.title}</span>
                </label>
              ))}
            </div>
          </details>
        </div>
      )}
    </div>
  )
}
```

- [ ] **Step 2: TS 빌드 검증**

```bash
cd frontend && npm run build
```

- [ ] **Step 3: Commit**

```bash
git add frontend/src/components/SearchBar.tsx
git commit -m "feat: SearchBar component with role/date/room filters"
```

---

### Task 7: SearchResults 컴포넌트 + 점프 트리거

**Files:**
- Create: `frontend/src/components/SearchResults.tsx`

**FR/Decision 매핑:** FR-3, FR-4, FR-5, FR-6, FR-9

- [ ] **Step 1: SearchResults — 결과 리스트 + 클릭 콜백 + 0건 안내**

```typescript
// frontend/src/components/SearchResults.tsx
import type { SearchResultItem } from "@/types"
import { Loader2 } from "lucide-react"

interface SearchResultsProps {
  query: string
  items: SearchResultItem[] | null
  total: number
  loading: boolean
  error: string | null
  canLoadMore: boolean
  onLoadMore: () => void
  onJump: (roomId: string, messageId: number, query: string) => void
}

const ROLE_BADGE: Record<string, string> = {
  user: "나",
  assistant: "AI",
  tool: "도구",
  system: "시스템",
}

function HighlightedSnippet({ snippet, query }: { snippet: string; query: string }) {
  if (!query) return <>{snippet}</>
  const lower = snippet.toLowerCase()
  const ql = query.toLowerCase()
  const idx = lower.indexOf(ql)
  if (idx < 0) return <>{snippet}</>
  return (
    <>
      {snippet.slice(0, idx)}
      <mark className="bg-yellow-200">{snippet.slice(idx, idx + query.length)}</mark>
      {snippet.slice(idx + query.length)}
    </>
  )
}

export function SearchResults({
  query, items, total, loading, error, canLoadMore, onLoadMore, onJump,
}: SearchResultsProps) {
  if (error) {
    return <div className="p-4 text-sm text-red-600">검색 중 오류: {error}</div>
  }
  if (items === null) {
    return null
  }
  if (items.length === 0) {
    return (
      <div className="flex h-full items-center justify-center p-8 text-sm text-muted-foreground">
        결과 없음 — 다른 키워드 또는 필터를 시도해 보세요.
      </div>
    )
  }
  return (
    <div className="flex-1 overflow-y-auto p-3 space-y-2">
      <p className="text-xs text-muted-foreground">총 {total}건</p>
      {items.map(it => (
        <button
          key={it.id}
          onClick={() => onJump(it.room_id, it.id, query)}
          className="block w-full rounded border p-2 text-left text-sm hover:bg-accent"
        >
          <div className="flex items-center justify-between text-xs text-muted-foreground">
            <span className="truncate">{it.room_title}</span>
            <span>
              [{ROLE_BADGE[it.role] ?? it.role}] {new Date(it.created_at).toLocaleString()}
            </span>
          </div>
          <div className="mt-1 break-words">
            <HighlightedSnippet snippet={it.snippet} query={query} />
          </div>
        </button>
      ))}
      {canLoadMore && (
        <button
          onClick={onLoadMore}
          disabled={loading}
          className="w-full rounded border py-2 text-xs hover:bg-accent disabled:opacity-50"
        >
          {loading ? <Loader2 className="mx-auto h-4 w-4 animate-spin" /> : "더 보기"}
        </button>
      )}
    </div>
  )
}
```

- [ ] **Step 2: TS 빌드 검증**

```bash
cd frontend && npm run build
```

- [ ] **Step 3: Commit**

```bash
git add frontend/src/components/SearchResults.tsx
git commit -m "feat: SearchResults component with highlight + jump trigger"
```

---

### Task 8: Sidebar 방 이름 필터

**Files:**
- Modify: `frontend/src/components/Sidebar.tsx`

**FR/Decision 매핑:** FR-8

- [ ] **Step 1: Sidebar 상단에 검색 input 추가, rooms는 props로 받은 그대로 필터링**

`Sidebar.tsx` 변경:
- `useState<string>("")` 로 `roomFilter` state 추가
- 렌더 직전 `const filtered = rooms.filter(r => r.title.toLowerCase().includes(roomFilter.toLowerCase()))`
- JSX 최상단에 `<input>` 추가 (placeholder="방 이름 검색…")
- 기존 rooms 렌더 루프는 `filtered`로 교체

구체적 diff:

```typescript
// Sidebar.tsx — useState import는 이미 됨
const [roomFilter, setRoomFilter] = useState("")

// 렌더 시작 부분에 추가
<div className="border-b p-2">
  <input
    type="text"
    value={roomFilter}
    onChange={e => setRoomFilter(e.target.value)}
    placeholder="방 이름 검색…"
    className="w-full rounded border px-2 py-1 text-xs"
    aria-label="방 이름 검색"
  />
</div>

// rooms.map(...) 전부를 filtered.map(...)로 교체
const filtered = rooms.filter(r => r.title.toLowerCase().includes(roomFilter.toLowerCase()))
```

- [ ] **Step 2: TS 빌드**

```bash
cd frontend && npm run build
```

- [ ] **Step 3: Commit**

```bash
git add frontend/src/components/Sidebar.tsx
git commit -m "feat: Sidebar room name filter (FR-8)"
```

---

### Task 9: ChatContainer scroll-to-message + 키워드 하이라이트

**Files:**
- Modify: `frontend/src/components/ChatContainer.tsx`
- Modify: `frontend/src/components/ChatMessage.tsx` (highlight 인자 받기)

**FR/Decision 매핑:** FR-4

- [ ] **Step 1: ChatContainer props 확장 + 점프 effect**

```typescript
// ChatContainer.tsx — props 추가
interface ChatContainerProps {
  messages: Message[]
  isWaitingResponse: boolean
  onApprove: () => void
  onReject: () => void
  onOpenFile: (path: string) => void
  pendingScroll?: { messageId: number; query: string } | null
  onScrolled?: () => void
}
```

```typescript
// 본문에 추가
const messageRefs = useRef<Map<number, HTMLDivElement>>(new Map())

useEffect(() => {
  if (!pendingScroll) return
  const el = messageRefs.current.get(pendingScroll.messageId)
  if (el) {
    el.scrollIntoView({ behavior: "smooth", block: "center" })
    el.classList.add("ring-2", "ring-yellow-400")
    setTimeout(() => el.classList.remove("ring-2", "ring-yellow-400"), 2500)
    onScrolled?.()
  }
}, [pendingScroll, messages, onScrolled])
```

기존 `useEffect`(scroll bottom)는 `pendingScroll`이 있으면 skip:

```typescript
useEffect(() => {
  if (pendingScroll) return
  bottomRef.current?.scrollIntoView({ behavior: "smooth" })
}, [messages, isWaitingResponse, pendingScroll])
```

각 메시지 렌더 시 ref 등록 — 단, `serverMessageId`가 있는 메시지만:

```typescript
{messages.map((msg) => {
  const setRef = (el: HTMLDivElement | null) => {
    if (msg.serverMessageId == null) return
    if (el) messageRefs.current.set(msg.serverMessageId, el)
    else messageRefs.current.delete(msg.serverMessageId)
  }
  // 기존 분기 (approval/file/chat) 각 wrapper에 ref={setRef} + data-msg-id 부착
  ...
})}
```

- [ ] **Step 2: ChatMessage에 highlight 받기**

```typescript
// ChatMessage.tsx — props에 highlightQuery?: string 추가
// content 렌더 시 highlightQuery가 있으면 <mark> 감싸서 표시
```

(SearchResults의 `HighlightedSnippet`을 공통 유틸 `frontend/src/lib/highlight.tsx`로 이동해 재사용 — 또는 props pass-through.)

- [ ] **Step 3: TS 빌드**

```bash
cd frontend && npm run build
```

- [ ] **Step 4: Commit**

```bash
git add frontend/src/components/ChatContainer.tsx frontend/src/components/ChatMessage.tsx
git commit -m "feat: scroll-to-message + ring highlight for FR-4 jump"
```

---

### Task 10: App.tsx 통합 — 검색 모드 ↔ 채팅 모드 전환

**Files:**
- Modify: `frontend/src/App.tsx`

**FR/Decision 매핑:** FR-1, FR-4 / 개발방향 §5 결정 4

- [ ] **Step 1: App.tsx state 확장**

```typescript
// App.tsx — 신규 state
const [searchFilter, setSearchFilter] = useState<SearchFilter>({
  q: "", roomIds: [], roles: [],
})
const [pendingScroll, setPendingScroll] = useState<{ messageId: number; query: string } | null>(null)

const search = useSearch(searchFilter)
const isSearchMode = searchFilter.q.length > 0

// 점프 핸들러
const handleJump = useCallback((roomId: string, messageId: number, query: string) => {
  setActiveRoomId(roomId)
  setSearchFilter(f => ({ ...f, q: "" }))  // 검색 모드 종료
  setPendingScroll({ messageId, query })
}, [])

const handleScrolled = useCallback(() => setPendingScroll(null), [])
```

- [ ] **Step 2: 레이아웃 — 상단 SearchBar + 메인영역 분기**

```tsx
return (
  <div className="flex h-screen">
    <Sidebar rooms={rooms} ... />
    <div className="flex flex-1 flex-col">
      <SearchBar
        rooms={rooms}
        filter={searchFilter}
        onChange={setSearchFilter}
        onClear={() => setSearchFilter({ q: "", roomIds: [], roles: [] })}
      />
      {isSearchMode ? (
        <SearchResults
          query={searchFilter.q}
          items={search.data?.items ?? null}
          total={search.data?.total ?? 0}
          loading={search.loading}
          error={search.error}
          canLoadMore={search.canLoadMore}
          onLoadMore={search.loadMore}
          onJump={handleJump}
        />
      ) : (
        <ChatContainer
          messages={messages}
          isWaitingResponse={isWaitingResponse}
          onApprove={handleApprove}
          onReject={handleReject}
          onOpenFile={handleOpenFile}
          pendingScroll={pendingScroll}
          onScrolled={handleScrolled}
        />
      )}
      {!isSearchMode && <ChatInput ... />}
    </div>
  </div>
)
```

- [ ] **Step 3: 메시지 로드 시 `serverMessageId` 매핑**

`useRooms.ts`(또는 메시지 fetch 위치)에서 `/rooms/{id}/messages` 응답의 `id`를 프론트 메시지에 mapping. 채팅 화면은 user/assistant만 렌더하므로 tool/system은 필터링:

```typescript
// 메시지 fetch 후 매핑 — UI에 띄울 type만 통과
const fetched: Message[] = serverMsgs
  .filter(sm => sm.type === "user" || sm.type === "assistant")
  .map(sm => ({
    id: `srv-${sm.id}`,
    type: sm.type as "user" | "assistant",
    content: sm.content,
    timestamp: new Date(sm.created_at).getTime(),
    serverMessageId: sm.id,
  }))
```

> 검색 결과 자체에는 tool/system도 표시(FR-2). 화면에 들어가서 메시지 리스트를 그릴 때만 user/assistant로 한정한다. 점프 대상 메시지가 tool/system이면 Task 9에서는 해당 ID가 ref Map에 등록되지 않으므로 자동 스크롤이 동작하지 않는데, MVP는 user/assistant 메시지로 점프하는 빈도가 압도적이므로 허용. 필요 시 후속 PR에서 ChatMessage union 확장.

- [ ] **Step 4: 빌드 + 수동 테스트 시나리오**

```bash
cd frontend && npm run build
```

수동 테스트 (서버 기동 후):
1. 새 방 생성 → "hello world" 메시지 입력 → AI 응답 대기
2. 다른 방 생성 → "react state" 메시지 입력
3. 상단 검색바에 "hello" 입력 → 결과 1건 표시 확인 (AC-1)
4. 결과 클릭 → 첫 번째 방으로 이동, 메시지로 자동 스크롤, 노란 테두리 (AC-2)
5. 사이드바 검색에 "처음 방 제목 일부" 입력 → 필터 (AC-3)
6. "오늘"/"최근 7일" 버튼 + role 체크박스 토글로 필터 변경 (AC-4)
7. "HELLO" 대문자 입력 → 동일 결과 (AC-5)
8. 존재하지 않는 키워드 → "결과 없음" 메시지 (AC-6)
9. 50개 이상 결과 시나리오는 mock 데이터로 별도 확인 (AC-7)

- [ ] **Step 5: Commit**

```bash
git add frontend/src/App.tsx frontend/src/hooks/useRooms.ts
git commit -m "feat: integrate search mode in App.tsx (search bar + results panel + jump)"
```

---

## 2. 위험 코드 지점

| 위치 | 카테고리 | 내용 | 완화 |
|---|---|---|---|
| `src/gp_claw/messages.py:append` | **side-effect** | `<tool_call>` 미정제 시 노이즈 검색결과 (R-5) | `_TOOL_TAG_RE.sub` 적용, 결과 빈 문자열이면 None 반환해 INSERT 스킵 |
| `src/gp_claw/messages.py:append` (트랜잭션) | **race** | 동일 room_id 동시 append 시 seq 충돌 (R-2) | `with self._conn:` 트랜잭션 + `(room_id, seq)` UNIQUE 인덱스. 충돌 시 IntegrityError → caller에서 재시도 또는 무시 |
| `src/gp_claw/server.py` user_message 핸들러 dual-write 호출 지점 (Task 2 Step 7) | **side-effect** | Mirror append 실패가 본 흐름에 영향 (R-1) | `try/except + logger.warning`으로 본 흐름 보호. backfill 재실행으로 사후 동기화 가능 |
| `src/gp_claw/server.py` `get_room_messages` 응답 스키마 | **breaking** | 응답에 `id`/`created_at` 추가로 기존 프론트 파서 깨질 가능 (R-7) | `frontend/src/types.ts`에 `serverMessageId?` 추가 + `tests/test_server.py::test_get_room_messages_returns_id_field` 회귀 |
| `src/gp_claw/messages.py:search` LIKE 절 | **perf** | 풀스캔 — 메시지 수 폭증 시 느려짐 (R-4) | NFR-1상 정량 SLA 없음, MVP 허용. Out-of-Scope FTS5는 후속 PRD |
| `src/gp_claw/server.py` `/search/messages` `limit/offset` | **perf** | 깊은 페이지에서 OFFSET 비용 (R-6) | MVP는 50개 단위, 보통 수십 페이지 미만이라 허용 |
| `scripts/backfill_messages.py:backfill` | **breaking** | 기존 사용자 첫 검색 결과 빈 화면 (R-3) | `python -m scripts.backfill_messages <db>` 1회 실행 안내 + README/HANDOFF.md 갱신 |

---

## 3. 롤백 전략

### Code 롤백
- 태스크별 커밋이 분리되어 있으므로 `git revert <SHA>`로 단계적 되돌림 가능.
- 전체 되돌림: `git revert <Task1 SHA>..<Task10 SHA>` 또는 브랜치 폐기.

### DB 롤백
- 신규 `messages` 테이블만 추가 — 기존 스키마(`rooms`, LangGraph 체크포인트 테이블) 변경 없음.
- 롤백 시: `sqlite3 <db_path> "DROP TABLE IF EXISTS messages;"` 1줄로 완료.
- 데이터 손실 영향: **없음** (mirror 테이블이며 진실 소스는 LangGraph 체크포인트).

### 프론트엔드 롤백
- `App.tsx`/`Sidebar.tsx`/`ChatContainer.tsx`/`ChatMessage.tsx` 변경 분량은 적당해서 `git revert`로 안전.
- `serverMessageId?` optional 필드 추가는 backward compatible — 신규 코드만 사용하므로 롤백해도 기존 메시지 흐름 영향 없음.

### Feature flag (선택)
- 본 MVP 범위에서는 별도 플래그 미도입. 필요하면 `App.tsx`에서 `import.meta.env.VITE_SEARCH_ENABLED === "true"` 가드로 쉽게 분리 가능.

### 테스트 한계 명시
- **프론트엔드는 Vitest/Jest 미설치** — 컴포넌트 단위 테스트 없음. `npm run build`(타입+빌드) + Task 10 Step 4의 9개 수동 시나리오로 검증한다. 자동화 테스트 도입은 본 PRD 범위 밖이며, 필요 시 후속 PR에서 별도 수행.

---

## 변경이력
<!-- change-history skill auto-appends entries here, oldest first -->

### [2026-05-02 18:27] [구현계획서-수정]
- **id**: CH-20260502-003
- **이유**: 신규 단계별 구현 계획 — 개발방향.md(CH-20260502-002)의 dual-store mirror 아키텍처를 10개 Task(백엔드 4 + 프론트 6)로 분해. verifying-spec gate에서 권장된 4건 반영: (1) `MessageStore.append`에 `IntegrityError` 1회 재시도, (2) `client_with_room_and_messages` fixture의 Task 3 의존성 명시, (3) Task 1에 NFR-1 sanity 측정 step 추가(10k mock < 1s), (4) §3 끝에 프론트 자동화 테스트 부재 명시.
- **무엇이**: 구현계획서.md 전체 신규 작성 — §1 Task 1~10 (각 5~12 bite-sized TDD steps), §2 위험 코드 지점 7행(R-1..R-7 매핑), §3 롤백 전략(Code/DB/FE/Feature flag/테스트 한계).
- **영향범위**: 없음 (최초 생성). 다운스트림 코드/테스트는 `/execute-plan` 단계에서 생성.
- **연관 항목**: CH-20260502-001 (요구사항), CH-20260502-002 (개발방향)

### [2026-05-02 18:58] [코드-수정] 전체 실행 완료 (Subagent-Driven Development)
- **id**: CH-20260502-004
- **이유**: 구현계획서.md(CH-20260502-003)의 10개 Task를 subagent-driven-development 방식으로 순차 실행. Task 1은 코드 리뷰 후 1건 보완(`IntegrityError` 무한재귀 → bounded 1-retry).
- **무엇이**: 11개 커밋으로 전체 피처 구현 완료
  - `a9f5107` Task 1: MessageStore mirror 클래스 + LIKE 검색 + 9 unit tests
  - `80886cc` Task 1 fix: IntegrityError 재시도 1회 bound + 무한재귀 회귀 테스트 추가
  - `bff9687` Task 2: WS dual-write (user + assistant + tool 메시지 mirror) + `get_room_messages` 응답에 `id`/`created_at` 추가 + `MessageStore.list_by_room()`
  - `e18b6b3` Task 3: `GET /search/messages` (필터/페이지네이션) + `GET /search/rooms` (방 이름 부분일치) + `app.state.message_store/room_manager` 노출
  - `2e52d5b` Task 4: `scripts/backfill_messages.py` (멱등 마이그레이션, SystemMessage 포함 4 role 매핑)
  - `809755e` Task 5: 프론트 타입 추가 (`MessageRole`, `SearchResultItem`, `SearchResponse`, `SearchFilter`) + `serverMessageId?` 필드 + `useSearch` hook (250ms 디바운스)
  - `a94dc4c` Task 6: `SearchBar` 컴포넌트 (role 체크박스, 날짜 quick-range, 방 다중선택)
  - `fce8f7f` Task 7: `SearchResults` 컴포넌트 (스니펫 하이라이트 + 점프 콜백 + 더보기)
  - `cbc37da` Task 8: `Sidebar` 방 이름 필터 (FR-8)
  - `341b896` Task 9: `ChatContainer` `pendingScroll`/`onScrolled` props + 메시지 ref Map + `ChatMessage` `highlightQuery` prop
  - `8186315` Task 10: `App.tsx` SearchBar/SearchResults 통합 + `handleJump` + `useWebSocket` 히스토리 로드에 `serverMessageId` 매핑
- **위험 카테고리**: side-effect (R-1, R-5 — dual-write try/except 보호), race (R-2 — `IntegrityError` bounded retry), breaking (R-3, R-7 — backfill 스크립트로 R-3 완화, `get_room_messages` 회귀 테스트로 R-7 검증), perf (R-4, R-6 — NFR-1 sanity 측정 0.003s/10k rows로 통과)
- **검증 결과**:
  - 백엔드 pytest: 94 passed / 2 failed (pre-existing `test_config` 환경 변수 leakage, 본 피처 무관)
  - 프론트엔드 `npm run build`: 0 errors / 0 warnings
  - 신규 테스트 13개 (test_messages: 11, test_server: 1, test_ws_agent: 1, test_search_api: 5, test_backfill: 2)
- **영향범위**:
  - 신규 파일 (8): `src/gp_claw/messages.py`, `scripts/__init__.py`, `scripts/backfill_messages.py`, `tests/test_messages.py`, `tests/test_search_api.py`, `tests/test_backfill.py`, `frontend/src/components/SearchBar.tsx`, `frontend/src/components/SearchResults.tsx`, `frontend/src/hooks/useSearch.ts`
  - 수정 파일 (8): `src/gp_claw/server.py`, `frontend/src/types.ts`, `frontend/src/components/Sidebar.tsx`, `frontend/src/components/ChatContainer.tsx`, `frontend/src/components/ChatMessage.tsx`, `frontend/src/App.tsx`, `frontend/src/hooks/useWebSocket.ts`, `tests/test_server.py`, `tests/test_ws_agent.py`
- **남은 작업**: 기존 사용자 DB가 있는 환경에서는 1회 `python -m scripts.backfill_messages <db_path>` 실행 필요(R-3 완화). Task 10 Step 4 수동 시나리오 9개 (브라우저 실제 점프/하이라이트/필터 동작)는 사용자 검수 단계.
- **연관 항목**: CH-20260502-003 (구현계획서)
